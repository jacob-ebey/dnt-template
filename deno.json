{
  "lock": false,
  "tasks": {
    "build": "deno run -A scripts/build-npm.ts 0.0.0",
    "format": "deno fmt mod.ts lib",
    "lint": "deno fmt mod.ts lib --check && deno lint mod.ts lib/",
    "test": "deno test lib/"
  },
  "exclude": ["npm/"],
  "imports": {
    "asserts": "https://deno.land/std@0.193.0/testing/asserts.ts"
  }
}
